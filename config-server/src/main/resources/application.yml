server:
  port: 8888

spring:
  application:
    name: config-server # Config Server의 이름
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SteveLee93/gw-config-repo
          default-label: main
